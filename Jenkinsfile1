pipeline {
    agent any
    
    environment {
        // Définir des variables d'environnement si nécessaire
    }

    stages {
        stage('Build') {
            steps {
                script {
                    echo 'Building the project...'
                    // Ajoutez vos étapes de build ici, par exemple :
                    // sh 'mvn clean install'
                }
            }
        }

        stage('Test') {
            steps {
                script {
                    echo 'Running tests...'
                    // Ajoutez vos étapes de test ici, par exemple :
                    // sh 'mvn test'
                }
            }
        }

        stage('Deploy') {
            steps {
                script {
                    echo 'Deploying the project...'
                    // Ajoutez vos étapes de déploiement ici
                }
            }
        }
    }

    post {
        always {
            echo 'Cleaning up...'
        }
        success {
            echo 'Pipeline succeeded!'
        }
        failure {
            echo 'Pipeline failed!'
        }
    }
}
